steps:
  # Build the container image
  - name: "gcr.io/cloud-builders/docker"
    args: 
      - "build"
      - "-t"
      - "${_REGION}-docker.pkg.dev/${PROJECT_ID}/spot-server/spot-server:${_VERSION}"
      - "."

  # Push the container image to Artifact Registry
  - name: "gcr.io/cloud-builders/docker"
    args: 
      - "push"
      - "${_REGION}-docker.pkg.dev/${PROJECT_ID}/spot-server/spot-server:${_VERSION}"

  # Wait before starting deployments
  - name: "gcr.io/cloud-builders/gcloud"
    id: wait-before-deployments
    entrypoint: sleep
    args: ["60"]  # 60 second delay before deployments start

  # Deploy shard 0
  - name: "gcr.io/cloud-builders/gcloud"
    id: deploy-shard-0
    waitFor: ["wait-before-deployments"]
    args:
      - "run"
      - "deploy"
      - "spot-server-shard-0"
      - "--image"
      - "${_REGION}-docker.pkg.dev/${PROJECT_ID}/spot-server/spot-server:${_VERSION}"
      - "--region"
      - "${_REGION}"
      - "--platform"
      - "managed"
      - "--allow-unauthenticated"
      - "--set-env-vars"
      - "PROJECT_ID=${PROJECT_ID},ENVIRONMENT=production,SHARD_COUNT=5,SHARD_INDEX=0"
      - "--memory"
      - "1Gi"
      - "--cpu"
      - "1"
      - "--concurrency"
      - "80"
      - "--cpu-boost"
      - "--no-cpu-throttling"
      - "--execution-environment"  
      - "gen2"
      - "--ingress"
      - "all"
      - "--session-affinity"
      - "--timeout"
      - "3600s"
      - "--min-instances"
      - "1"
      - "--max-instances"
      - "1"

  # Deploy shard 1
  - name: "gcr.io/cloud-builders/gcloud"
    id: deploy-shard-1
    waitFor: ["wait-before-deployments"]
    args:
      - "run"
      - "deploy"
      - "spot-server-shard-1"
      - "--image"
      - "${_REGION}-docker.pkg.dev/${PROJECT_ID}/spot-server/spot-server:${_VERSION}"
      - "--region"
      - "${_REGION}"
      - "--platform"
      - "managed"
      - "--allow-unauthenticated"
      - "--set-env-vars"
      - "PROJECT_ID=${PROJECT_ID},ENVIRONMENT=production,SHARD_COUNT=5,SHARD_INDEX=1"
      - "--memory"
      - "1Gi"
      - "--cpu"
      - "1"
      - "--concurrency"
      - "80"
      - "--cpu-boost"
      - "--no-cpu-throttling"
      - "--execution-environment"  
      - "gen2"
      - "--ingress"
      - "all"
      - "--session-affinity"
      - "--timeout"
      - "3600s"
      - "--min-instances"
      - "1"
      - "--max-instances"
      - "1"

  # Deploy shard 2
  - name: "gcr.io/cloud-builders/gcloud"
    id: deploy-shard-2
    waitFor: ["wait-before-deployments"]
    args:
      - "run"
      - "deploy"
      - "spot-server-shard-2"
      - "--image"
      - "${_REGION}-docker.pkg.dev/${PROJECT_ID}/spot-server/spot-server:${_VERSION}"
      - "--region"
      - "${_REGION}"
      - "--platform"
      - "managed"
      - "--allow-unauthenticated"
      - "--set-env-vars"
      - "PROJECT_ID=${PROJECT_ID},ENVIRONMENT=production,SHARD_COUNT=5,SHARD_INDEX=2"
      - "--memory"
      - "1Gi"
      - "--cpu"
      - "1"
      - "--concurrency"
      - "80"
      - "--cpu-boost"
      - "--no-cpu-throttling"
      - "--execution-environment"  
      - "gen2"
      - "--ingress"
      - "all"
      - "--session-affinity"
      - "--timeout"
      - "3600s"
      - "--min-instances"
      - "1"
      - "--max-instances"
      - "1"

  # Deploy shard 3
  - name: "gcr.io/cloud-builders/gcloud"
    id: deploy-shard-3
    waitFor: ["wait-before-deployments"]
    args:
      - "run"
      - "deploy"
      - "spot-server-shard-3"
      - "--image"
      - "${_REGION}-docker.pkg.dev/${PROJECT_ID}/spot-server/spot-server:${_VERSION}"
      - "--region"
      - "${_REGION}"
      - "--platform"
      - "managed"
      - "--allow-unauthenticated"
      - "--set-env-vars"
      - "PROJECT_ID=${PROJECT_ID},ENVIRONMENT=production,SHARD_COUNT=5,SHARD_INDEX=3"
      - "--memory"
      - "1Gi"
      - "--cpu"
      - "1"
      - "--concurrency"
      - "80"
      - "--cpu-boost"
      - "--no-cpu-throttling"
      - "--execution-environment"  
      - "gen2"
      - "--ingress"
      - "all"
      - "--session-affinity"
      - "--timeout"
      - "3600s"
      - "--min-instances"
      - "1"
      - "--max-instances"
      - "1"

  # Deploy shard 4
  - name: "gcr.io/cloud-builders/gcloud"
    id: deploy-shard-4
    waitFor: ["wait-before-deployments"]
    args:
      - "run"
      - "deploy"
      - "spot-server-shard-4"
      - "--image"
      - "${_REGION}-docker.pkg.dev/${PROJECT_ID}/spot-server/spot-server:${_VERSION}"
      - "--region"
      - "${_REGION}"
      - "--platform"
      - "managed"
      - "--allow-unauthenticated"
      - "--set-env-vars"
      - "PROJECT_ID=${PROJECT_ID},ENVIRONMENT=production,SHARD_COUNT=5,SHARD_INDEX=4"
      - "--memory"
      - "1Gi"
      - "--cpu"
      - "1"
      - "--concurrency"
      - "80"
      - "--cpu-boost"
      - "--no-cpu-throttling"
      - "--execution-environment"  
      - "gen2"
      - "--ingress"
      - "all"
      - "--session-affinity"
      - "--timeout"
      - "3600s"
      - "--min-instances"
      - "1"
      - "--max-instances"
      - "1"

substitutions:
  _REGION: europe-west1

images:
  - "${_REGION}-docker.pkg.dev/${PROJECT_ID}/spot-server/spot-server:${_VERSION}"

timeout: 1800s
